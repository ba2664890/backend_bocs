# Configuration FATI Backend

# Django
DEBUG=True
SECRET_KEY=change-this-to-a-secure-secret-key-in-production
ALLOWED_HOSTS=localhost,127.0.0.1,api.fati.gov.sn

# Database
DB_NAME=fati_db
DB_USER=postgres
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432

# CORS (Frontend URLs)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://fati.gov.sn

# Email (optionnel)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# Cache (optionnel - Redis)
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache
CACHE_LOCATION=
REDIS_URL=

# Celery (optionnel)
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=
